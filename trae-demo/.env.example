# SlideForge AI Demo Studio 环境变量示例
# 复制本文件为 .env 并按需修改（后端可选，AI Key 在前端界面填写）

# 后端服务端口
PORT=5000

# 后端服务主机（默认 127.0.0.1，仅本机访问；改为 0.0.0.0 可局域网访问）
HOST=127.0.0.1

# 是否开启 Flask 调试模式（生产环境建议 False）
DEBUG=False

# 注意：AI API Key 不需要在这里配置
# AI Key 在浏览器界面「第 0 步：配置」中填写，只存在浏览器 localStorage
